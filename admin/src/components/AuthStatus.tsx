'use client';

// This component is no longer needed with the simplified auth system
// The SimpleAuthContext doesn't expose complex auth errors or retry mechanisms
export default function AuthStatus() {
  return null;
}